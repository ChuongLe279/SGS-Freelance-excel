services:
  - type: web
    name: sgs-excel-api
    env: python
    buildCommand: pip install -r backend-requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        value: https://silver-faun-7aac4a.netlify.app
